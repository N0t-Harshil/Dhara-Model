# =============================================================================
# Dhara Class Model — Dependencies
# =============================================================================
# JupyterLab server (GPU training):
#   pip install -r requirements.txt
#
# Windows PC (development, no GPU):
#   pip install -r requirements-dev.txt
# =============================================================================

# PyTorch with CUDA 12.8+ (Blackwell / H100 / A100 / RTX 4090+)
# NOTE: cu128 wheels start at torch 2.7.0 (2.6.0+cu128 does not exist on the index)
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.9.0+cu128
torchvision==0.24.0+cu128
torchaudio==2.9.0+cu128

# HuggingFace ecosystem (verified working set — code uses eval_strategy + AutoConfig.for_model, requires >=4.46)
transformers==5.1.0
datasets==4.0.0
accelerate==1.12.0
bitsandbytes>=0.43.0
peft>=0.10.0
tokenizers==0.22.2

# Core utilities
pyyaml
pydantic>=2.0.0
tqdm
sentencepiece
scipy
numpy>=1.24
requests>=2.31
psutil>=5.9

# Testing (required for `python main.py test`)
pytest>=7.0

# GraphRAG & Agentic Memory
chromadb
sentence-transformers
networkx

# Web Chat UI (unused by current code; kept only if you run an external UI)
# flask>=3.0.0

# Optional: ~2x attention speed on A100/H100/Blackwell
# flash-attn --no-build-isolation

# Experiment tracking (optional — uncomment as needed)
# wandb
# mlflow
# tensorboard

# Evaluation (optional — uncomment as needed)
# human-eval
# pylint
