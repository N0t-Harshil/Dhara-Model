# Known Issues

Auto-identified from code analysis. Each issue includes root cause, impact, suggested fix, and priority.

---

| # | Issue | File | Root Cause | Impact | Suggested Fix | Priority |
|---|-------|------|------------|--------|---------------|----------|
| 1 | GAIR/MathPile is gated | `src/data/registry.py:338` | Dataset requires HuggingFace access request approval | 0.010 weight (10% of math category) will fail to load unless token has approved access | Request access at https://huggingface.co/GAIR/MathPile or replace with open-web-math backup | HIGH | **RESOLVED**: User has token access to GAIR/MathPile — 100% coverage achievable |
| 2 | PyTorch docs behind Cloudflare | `src/data/doc_builder.py:335` | Cloudflare anti-bot challenge prevents HTTP fetching | 0 pages from pytorch-docs scraper despite genindex discovery strategy | Use readthedocs.io mirror (https://pytorch.readthedocs.io) or integrate `cloudscraper` library | HIGH |
| 3 | RFC index 16MB download | `src/data/doc_builder.py:622` | Original RFC HTML index was a 16MB Nuxt SSR page | Slow downloads and frequent timeouts during scraping | **FIXED**: Switched to IETF text index (`rfc-index.txt`) which is significantly smaller. Keep monitoring for regression. | HIGH |
| 4 | PostgreSQL docs navigation changed | `src/data/doc_builder.py:442` | Site now uses Bootstrap with JS-rendered navigation | Only 1 page scraped (the index) instead of 300+; link discovery cannot follow JS-generated links | Needs new discovery logic: use the sitemap XML or alternative HTML version. Investigate `https://www.postgresql.org/docs/current/static/` as alternative path | MEDIUM |
| 5 | cuDNN 90% link failure | `src/data/doc_builder.py:790` | Sub-links in cuDNN docs don't exist as standalone HTML pages | 66 failed requests per scrape; wastes time and bandwidth | **FIXED**: Limited to 6 seed pages with sub-link following disabled. Keep monitoring — NVIDIA may restructure their docs. | MEDIUM |
| 6 | Many tests skipped/scaffolded | `tests/` | Test suite was scaffolded with placeholder implementations | Low test coverage — many tests pass trivially without actually testing logic | Fill in test implementations. Priority targets: training pipeline, data pipeline, alignment, evaluation, quality filtering | HIGH |
| 7 | config_foundation.yaml not tracked in version control | `config_foundation.yaml` | File exists on disk but may not be in `.gitignore` or tracking is inconsistent | Every config change must be manually communicated; no history of changes | Run `git add config_foundation.yaml` to track it, or if intentional exclusion, generate it from defaults programmatically | MEDIUM |
| 8 | Sparse test coverage for training pipeline | `tests/test_trainer.py`, `tests/test_foundation_pipeline.py` | Training tests use minimal placeholder models (tiny configs) and only check forward pass | Core training loop (optimizer stepping, gradient accumulation, FSDP integration, checkpoint resume) is untested | Add training loop tests: (1) 10-step training with loss decrease verification, (2) checkpoint save/load/resume, (3) gradient accumulation correctness | HIGH |
| 9 | Docker/Kubernetes docs low page count | `src/data/doc_builder.py:418,431` | Possibly JS-based navigation, rate limiting, or aggressive link following restrictions | Only 9-12 pages scraped from Docker docs and ~50 from Kubernetes instead of expected 200+ | Investigate with browser DevTools: check for JS-rendered navigation, sitemap.xml, or alternative docs layout. May need Playwright/Selenium for proper scraping | LOW |
| 10 | MassiveDataCollector legacy wrapper | `src/data/streaming.py:561` | Backward compatibility wrapper that reconstructs registry entries inline | Dead code that bypasses the proper DatasetRegistry fallback system; deprecated in favor of StreamingManager | Remove after migration is complete. Search for all `collector` usages and replace with `stream_dataset_with_fallbacks` + `DatasetRegistry` | LOW |
| 11 | Some _register_* functions return unrounded floats | `src/data/registry.py:205-413` | Floating-point arithmetic in weight calculations across multiple registration functions | Tiny weight drift (e.g., 0.3004 instead of 0.30) that accumulates across categories | Use `round()` consistently at each registration step or use `Decimal` for exact arithmetic | LOW |
| 12 | NSLT tests may not validate correctness | `tests/test_nslt.py` | Tests only check forward pass output shapes and basic gradient flow | Architecture correctness (SSM recurrence, ODE solver accuracy, sparse gating) is untested | Add: (1) gradient flow test for each sub-module, (2) output range/constraint tests, (3) numerical stability tests (NaN/Inf detection), (4) backward pass correctness with fixed random seeds | MEDIUM |
| 13 | HF token hardcoded in config_foundation.yaml | `config_foundation.yaml` (historical) | Token strings were written directly in the config file (values redacted; both must be revoked on huggingface.co) | Security risk: token committed to version control | FIXED: `data.hf_token` is now empty in all configs; set the `HF_TOKEN` environment variable instead. Revoke the old tokens on hf.co and consider git history scrubbing | RESOLVED (code side) |
| 14 | Batch size mismatch in config comments | `config.yaml:96-98` | Comment says "effective batch = 2 × 4 × 8 = 64" but `batch_size: 2` × `gradient_accumulation_steps: 8` × 4 GPUs = 64 | Documentation inconsistency — no runtime impact but misleading | Update comment to reflect actual formula or verify against Trainer's effective batch calculation | LOW |
| 15 | DataPipeline uses MassiveDataCollector for non-registry mode | `src/data/pipeline.py:413-416` | `DataPipeline.collector` property lazily creates a `MassiveDataCollector` when `use_registry` is false | Non-registry dataset loading bypasses quality filtering, dedup, and packing | Ensure consistent behavior between registry and non-registry modes, or deprecate non-registry mode entirely | LOW |
| 16 | Thread safety in doc_builder | `src/data/doc_builder.py:735-743` | `scrape_all()` uses threading without locks for shared state | `_visited`, `_failed`, counters may have race conditions if scraping is made concurrent within a single scraper | Currently each scraper runs in its own thread, but single-threaded internally. If future changes add intra-scraper concurrency, add threading locks | LOW |
| 17 | Health report JSON serialization | `src/data/health_reporter.py:288-299` | Uses `default=str` for non-serializable types | May produce non-standard JSON for some edge cases | Use explicit serialization for each field type, or use Pydantic models for report entries | LOW |
| 18 | ExperimentTracker does not support multiple simultaneous runs | `src/infrastructure/tracking.py:14` | Single `_run` attribute; re-initializing overwrites previous run | Cannot track multiple experiments from same process | Support multiple run IDs or process-level tracking tags | LOW |
| 19 | `idx` shadows outer loop in `build_pretrain_dataset_from_registry` | `src/data/pipeline.py:956` | Inner `for idx, text in enumerate(cleaned_texts)` overwrites outer `for idx, info in enumerate(all_infos, 1)` | Low risk — outer `idx` only used before inner loop. **FIXED**: renamed to `text_idx` | HIGH |
| 20 | Empty text logged under wrong counter | `src/data/pipeline.py:881` | Empty text used `rejected_boilerplate` instead of dedicated `rejected_empty` counter | Ledger statistics misleading. **FIXED**: changed to `rejected_empty` | MEDIUM |
| 21 | Inconsistent `_file_path` vs `file_path` key | `src/data/pipeline.py:885` | Registry method used `_file_path` but non-registry method used `file_path` | One path would skip 100% boilerplate checks if wrong key used. **FIXED**: both now use `file_path` | MEDIUM |
| 22 | Phase 8 calls missing `scripts/run_training.py` | `scripts/production_validation.py:521` | Script doesn't exist — Phase 8 crashes on every run | Full validation suite can never complete. **FIXED**: added existence check, returns SKIPPED with clear reason | HIGH |
| 23 | `total_issues` undefined in report generator | `scripts/production_validation.py:670` | `total_issues` only defined inside `if ds:` block — `NameError` if Phase 5 returns empty/falsy | Report generation crashes if Phase 5 produces no samples. **FIXED**: initialized to 0 before the block | HIGH |
| 24 | `_fetch()` silently swallows all errors | `src/data/doc_builder.py:88` | Catches all exceptions and returns `None` with zero logging | Scrapers fail invisibly — produce 0 pages with no error indication. **FIXED**: Added per-attempt WARNING logging with URL, attempt count, and error type | CRITICAL |
| 25 | Local JSONL verification skipped entirely | `scripts/verify_datasets.py:89-95` | `path.startswith("json")` entries marked `local_json` without verifying file exists | 17/18 missing doc sources counted as available. **FIXED**: `_verify_local_dataset()` checks dir, file, UTF-8, JSONL validity, schema, record count. Uses valid_record_ratio-based tolerance: ≥95% + no errors = `ok_local`, ≥50% or clean schema warnings = `warning_local`, <50% = `invalid_local` | CRITICAL |
| 26 | Category accounting hides `skipped_dup` entries | `scripts/verify_datasets.py:130-160` | `report()` shows OK column as only `ok` status (excludes `skipped_dup`) | Books/Science/Structured show "0 OK" despite available weight. **FIXED**: OK column now shows `ok + skipped_dup` combined, weight reflects all available | HIGH |
| 27 | Production validation counts `local_json` as available | `scripts/production_validation.py:35` | Uses old `local_json` status which is no longer emitted | Missing local docs inflate available corpus weight. **FIXED**: Changed filter to `("ok", "skipped_dup", "ok_local")` — only verified local docs count | CRITICAL |
